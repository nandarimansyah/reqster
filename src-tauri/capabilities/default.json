{
  "$schema": "../gen/schemas/desktop-capability.json",
  "identifier": "default",
  "description": "Default capability for the main Reqster window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
